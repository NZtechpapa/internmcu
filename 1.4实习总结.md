#任务
- 对于项目的一个功能GPIO的输入输出，对其中的串口通信进行了阅读与理解。
- 此项目采用中断接收方式，第一步：HAL库初始化，第二步：系统时钟配置，第三步：外设硬件初始化，第四步：串口上层软件初始化，然后进行串口的主任务执行，第一步：检测串口接收完成标志，第二步：解析串口接收的数据，第三步：若解析成功，执行 GPIO 控制指令，第四步：处理串口接收的原始数据。然后通过回调函数依次读入数据。
#遇到的难题
- 对于代码看懂和逻辑搞通基本搞定，就是功能调试的时候缺少一些装备，没有调试成功。
#明日计划
- 对于单片机我想快速入手下项目，看看自己差距在哪里，干中带学，我相信越努力越幸运。





