#今日所悟
我有个芯片手册我测试他的功能顺序，先看芯片手册然后根据芯片手册的说明，通过stm32cubemx建议基础测试框架代码，然后用Keil打开，在kev打开的文件基础上拓展我想要测试的功能
### 测试逻辑整体思路

#### 一、代码验证

1. 静态验证：看链接结果

- 编译后查看 .map 或用 Watch 表达式：

- &CCMRAM_TestFunction

- &EXTI0_IRQHandler

- 预期：地址都在 0x10000000 ~ 0x10000FFF，说明链接脚本和 section 属性生效，代码段位于 CCM RAM。

1. 运行时验证：全局变量 func_addr

- 在 main() 中执行：func_addr = CCMRAM_GetFunctionAddress();

- 在这行之后查看 func_addr。

- 预期：func_addr == &CCMRAM_TestFunction，且在 0x1000xxxx 范围。

---

#### 二、普通函数执行验证

1. 逻辑设计

- 在 CCMRAM_TestFunction() 里只做一件事：ccmram_test_counter++。

- 在 main()：

- 初始化后调用一次 CCMRAM_TestFunction()。

- 在 while(1) 里每隔 1s 再调用一次。

1. 测试动作

- 在调试器里观察：

- 初次调用后 ccmram_test_counter 从 0 变为 1。

- 循环运行时，每秒递增 1。

1. 预期结果

- 计数器稳定按时间递增，且 &CCMRAM_TestFunction 始终在 0x1000xxxx。

- 说明：代码既在 CCM RAM 里，又可以反复正常执行。

---

#### 三、外部中断 + CCM 回调验证

1. 逻辑设计

- EXTI0_IRQHandler() 标记为 CCMRAM_FUNC，被链接到 CCM RAM

- 在 EXTI0_IRQHandler() 里

- 调用 CCMRAM_InterruptCallback()（同样在 CCM RAM）。

- CCMRAM_InterruptCallback() 内部

- ccmram_interrupt_flag = 1;

- ccmram_test_counter++

1. 测试动作

- 在 EXTI0_IRQHandler() 和 CCMRAM_InterruptCallback() 内各下一个断点。

- 运行程序，外部通过按键/跳线触发 PA0 中断。

- 每次触发中断：

- 程序首先停在 EXTI0_IRQHandler() 的断点；

- 在 CPU 寄存器窗口查看 PC，确认地址在 0x1000xxxx。

- 继续单步，到 CCMRAM_InterruptCallback() 断点；

- 查看 PC 仍在 0x1000xxxx。

- 观察 ccmram_interrupt_flag 从 0 变为 1，ccmram_test_counter 递增。

1. 预期结果

- 每次外部中断：

- 断点都能命中；

- PC 在 0x1000xxxx；

- 标志位和计数器变化正确。

- 说明：中断入口和中断处理逻辑都从 CCM RAM 执行且功能正确。

---

### 判定标准（测试通过条件）

- 地址正确：

- &CCMRAM_TestFunction、&EXTI0_IRQHandler ∈ \[0x10000000, 0x10000FFF]。

- func_addr == &CCMRAM_TestFunction。

- 普通调用正常：

- 不触发中断时，只靠 while(1) 调用，ccmram_test_counter 随时间单调递增。

- 中断调用正常：

- 触发 EXTI0 中断时：

- 命中 CCM RAM 中的中断断点；

- ccmram_interrupt_flag 由 0 变 1；

- ccmram_test_counter 在中断里额外递增。

只要这三块都满足，就可以说明 CCM RAM 执行普通函数和中断程序”的功能已经按设计实现并通过验证。